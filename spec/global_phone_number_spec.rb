require "spec_helper"

RSpec.describe GlobalPhoneNumber do
  it "has a version number" do
    expect(GlobalPhoneNumber::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
